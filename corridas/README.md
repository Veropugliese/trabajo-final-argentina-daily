# Corridas oficiales

Acá van las tres corridas reales del trabajo final, generadas con la v3 del contrato (`prompts/system_prompt.md`). Cada archivo tiene tres partes: **entrada** (el `user_prompt.md` completo con los valores reales de esa corrida), **salida** (el JSON tal cual lo devolvió Gemini) y **fecha** (cuándo se ejecutó).

Pendiente: generar las tres corridas ejecutando `scripts/daily_briefing.py` (o el pedido equivalente a mano) con la API key real.
